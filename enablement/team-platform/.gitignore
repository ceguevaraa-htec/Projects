# HTEC kit tooling (commands/templates only — no project artifacts live here,
# confirmed via create-brief.md: its output goes to /specs, not into htec-kit/)
htec-kit/

# OS
.DS_Store

# Editor
.vscode/*
!.vscode/settings.json

# Node.js / TypeScript (per SDP: Express + Kysely + Vitest, picked in /speckit.plan)
node_modules/
dist/
build/
*.log
.env
.env.*
coverage/

# SQLite database (per SDP ADR-0008: always regenerated via the seed script, never committed)
*.db
*.db-journal
*.db-wal
*.db-shm